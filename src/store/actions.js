

export default {
  //获取用户信息
  getUser({commit}) {
    // return get_account_profile().then(res => {
    //   let data = res.data
    //   if (data.code === '000') {
    //     let user = data.content;
    //     commit('SET_USER', user)
    //   } else {
    //     commit('SET_USER', {})
    //   }
    // }).catch(err => {
    //   commit('SET_USER', {})
    //   console.log(err);
    // })
  },
}
